buildscript {
    ext {
        plugins {
            id 'java'
        }
        
        springBootVersion = '2.1.7.RELEASE'

        group 'com.eightball.book'
        version '1.0-SNAPSHOT'

        repositories {
            mavenCentral()
        }
    }
}

dependencies {
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.7.0'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.7.0'
}

test {
    useJUnitPlatform()
}